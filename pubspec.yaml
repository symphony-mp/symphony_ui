name: symphony_ui
description: "An open source music player that contains the main local and offline features."

homepage: https://softyes.com.br
repository: https://github.com/symphony-mp/symphony_ui

version: 0.0.1

environment:
  sdk: ^3.9.2
  flutter: ^3.35.6

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  very_good_analysis: ^10.0.0
